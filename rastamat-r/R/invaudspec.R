# In Development
invaudspec <- function(aspectrum, sr = 16000, nfft = 512, fbtype = 'bark', minfreq = 0, maxfreq = -1, sumpower = 1, bdwidth = 1){

  if (maxfreq == -1) {
    maxfreq <- sr/2
  }

  # return(list(spec=spec, wts=wts, iwts=iwts))
}
